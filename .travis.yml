sudo: required
dist: trusty

language: node_js
node_js:
  - "9"

services:
  - docker

cache:
  directories:
    - environ/lpg-ansible-mvp
    - node_modules
    - pyhome
    - test/headless/node_modules

env:
  - ENV_PROFILE=dev

install: true
script: ./environ/ci
